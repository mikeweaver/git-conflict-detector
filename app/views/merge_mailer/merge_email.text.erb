<%= render partial: 'merge_list', locals: {merges: @successful_merges, success: true} %>

<%= render partial: 'merge_list', locals: {merges: @unsuccessful_merges, success: false} %>

You are receiving this email because you made a commit to the branches listed above in the <%= @repository_name %> repository.
To unsubscribe, click here: <%= url_for controller: :users, action: :new_unsubscribe, id: @user.id %>
