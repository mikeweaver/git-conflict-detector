Conflicts detected on updated branches:
<%= render partial: 'conflict_list', locals: {conflicts: @new_conflicts} %>

Conflicts that have been resolved:
<%= render partial: 'conflict_list', locals: {conflicts: @resolved_conflicts} %>

Existing conflicts on untouched branches:
<%= render partial: 'conflict_list', locals: {conflicts: @existing_conflicts} %>
